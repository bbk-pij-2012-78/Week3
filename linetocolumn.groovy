String sInput = '';
int x = 0;
int sp = 0;
int ep = 0;

/*
print('Enter some text: ');
sInput = System.console().readLine();

while (x < sInput.length()) {
    println(sInput.charAt(x));
    x++;
}

while (sp != -1) {
    if (sp == 0) {
        sp = sInput.indexOf(' ');
        println(sp);
        if (sp != -1) {
            println(sInput.substring(0, sp));
        }
    } else {
	ep = sInput.substring(' ', sp)
        println(sInput.substring(ep, sp);
        sp += sInput.indexOf(' ', sp);
    }
    println(sp);
}
*/

sInput = 'the quick brown fox'
println(sInput.length());
println(sInput.substring(0,3));
println(sInput.substring(4,9));
println(sInput.substring(10,15));